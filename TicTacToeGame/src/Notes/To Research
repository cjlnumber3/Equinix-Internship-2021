1.) for (int row = 0; row < numberOfGameBoardRows; ++row) {
                System.arraycopy(Main.gameBoard[row], 0, mockGameBoard[row], 0, numberOfGameBoardColumns);
            }

            for copying an array over to another 2D array using System.arraycopy?

2.) Junit try-catch blocks for constructed objects

3.) @Before & @BeforeClass

4.) Mass ignore suggestions? (Yellow tabs on FairyFloss)

5.) Write tests IN TANDEM while doing abstraction to basically test it until it fails. Basically test until you haven't got parameters left.